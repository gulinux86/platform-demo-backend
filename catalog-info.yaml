apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: platform-demo-backend
  title: Platform Demo — Backend API
  description: REST API built with FastAPI, deployed on Cloud Run via the GCP Serverless Platform golden path.
  tags:
    - python
    - fastapi
    - cloud-run
    - postgresql
  annotations:
    github.com/project-slug: gulinux86/platform-demo-backend
    backstage.io/techdocs-ref: dir:.
spec:
  type: service
  lifecycle: experimental
  owner: group:default/platform-team
  system: platform-demo
